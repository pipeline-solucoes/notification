import { ThemeOptions } from '@mui/material/styles';

export const notificationThemeOptions: ThemeOptions = {
  pipelinesolucoes: {
    notification: {
        background: '#fff', 
        color: "#000",
        borderRadius: "0",
        boxShadow: "none",
        borderColor: "#ccc",
        padding: "4px 8px",      
    }
  },
};